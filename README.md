# @prodaric/shell

Prodaric Framework — Eclipse Theia shell (panel registry, layout provider, frontend module).

## Instalación

```bash
npm install @prodaric/shell
```

Requiere configuración de `@prodaric:registry=https://npm.pkg.github.com/` y autenticación en GitHub Packages.
